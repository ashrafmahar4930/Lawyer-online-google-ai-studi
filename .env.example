# Gemini API Key for AI features (Legal drafting, QA moderate & auto-answers)
GEMINI_API_KEY=

# Firebase / Firestore configurations (optional, falls back to config json if present)
FIREBASE_PROJECT_ID=
FIRESTORE_DATABASE_ID=
